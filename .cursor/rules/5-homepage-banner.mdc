---
alwaysApply: true
---
# Zenava 项目 - 首页Banner轮播系统修改规则

## Banner定位
- **翻译文件**: `src/i18n/translations.ts` (第 1327+ 行，`banners` 配置)
- **配置文件**: `src/pages/HomepageDB.tsx` (第 72-166 行左右，`const bannerSlides = [ ... ]`)
- **组件功能**: 首页顶部轮播横幅，展示核心价值主张和产品特性
- **支持语言**: 简体中文(zh)、英文(en)、日文(jp)、繁体中文(hk)
- **主要输入语言**: 简体中文 (zh) - 其他语言自动生成

## 🎯 数据架构（重要！）
- **翻译数据**: 所有文字内容（官网名称、标题、描述、按钮文字、图片alt）统一存放在 `src/i18n/translations.ts` 的 `banners` 配置中
- **配置数据**: 背景色、图片路径、链接、图标等配置保留在 `HomepageDB.tsx` 中
- **使用方式**: `HomepageDB.tsx` 通过 `trans.banners.slide1.mainTitle` 读取翻译

## Banner功能特性

### 🎬 轮播功能
- ✅ **自动翻页**: 每隔10秒自动切换到下一页
- ✅ **翻页顺序**: 从左到右（从第1页到最后一页，循环）
- ✅ **手动控制**: 点击底部翻页指示器直接跳转
- ✅ **重置计时**: 手动点击后重新开始10秒倒计时
- ✅ **最多8页**: 超过8个Banner时，总是显示最新的8个
- ✅ **单页优化**: 只有1页时，翻页图标不显示，自动翻页功能不生效

### 🎨 布局结构

Banner 支持两种布局模式：

#### 模式1：左文右图布局（`layout: 'split'`，默认）
- **左侧**: 文字内容区（官网名称、主标题、概述、按钮）
- **右侧**: 图片或组件区（支持图片或AI对话模拟组件）
- **背景**: 渐变色 + 可选网格图案

#### 模式2：全图布局（`layout: 'full-image'`，新增）
- **背景**: 直接使用一张完整的 Banner 图片
- **文字内容**: 可选，可叠加在图片上
- **文字位置**: 支持配置（居中、左上、右下等）
- **遮罩层**: 可选，用于提高文字可读性

- **指示器**: 底部居中显示翻页圆点（两种模式通用）

---

## Banner数据结构说明

### 📁 文件 1：翻译文件 (`src/i18n/translations.ts`)

```typescript
// 在 siteTranslations 的每个语言（zh/en/jp/hk）中添加：
banners: {
  slide1: {
    siteName: 'ZENAVA',                           // 官网名称
    mainTitle: '企业级AI对话解决方案',             // 主标题
    description: '通过智能AI助手转变您的客户服务...', // 描述（50-80字）
    buttonText: '预约咨询',                        // 按钮文字
    imageAlt: '横幅图片1'                          // 图片描述
  },
  slide2: { ... },
  slide3: { ... },
  // 最多8个 slide
}
```

### 📁 文件 2：配置文件 (`src/pages/HomepageDB.tsx`)

#### 模式1：左文右图布局（`layout: 'split'`）

```typescript
// Banner 数据配置（支持最多8页）
const bannerSlides = [
  {
    // 唯一标识符（必需）
    id: 'slide-1',
    
    // 布局模式（必需）
    layout: 'split',  // 'split' 或 'full-image'
    
    // 背景配置（仅 split 模式使用）
    background: {
      gradient: 'from-white via-[#f0ebff] to-[#5E3AFC]',  // Tailwind CSS 渐变类名
      pattern: true                                        // 是否显示网格图案（true/false）
    },
    
    // 左侧文字内容（从 translations.ts 读取，仅 split 模式使用）
    leftContent: {
      siteName: trans.banners.slide1.siteName,           // 从翻译读取
      mainTitle: trans.banners.slide1.mainTitle,         // 从翻译读取
      description: trans.banners.slide1.description,     // 从翻译读取
      button: {
        text: trans.banners.slide1.buttonText,           // 从翻译读取
        link: '#contact',                                // 按钮链接（配置）
        icon: 'fas fa-phone'                             // 按钮图标（配置）
      }
    },
    
    // 右侧图片或组件（仅 split 模式使用）
    rightImage: {
      type: 'image',                                     // 'image' 或 'component'
      src: '/assets/images/banner-1.png',                // 图片路径（配置）
      alt: trans.banners.slide1.imageAlt                 // 从翻译读取
      
      // 如果 type === 'component'
      // type: 'component',
      // component: 'ai-simulation'  // 显示AI对话模拟组件
    }
  }
]
```

#### 模式2：全图布局（`layout: 'full-image'`）

```typescript
{
  id: 'slide-5',
  
  // 布局模式（必需）
  layout: 'full-image',  // 全图模式
  
  // 全图配置
  fullImage: {
    src: '/assets/images/banner-full-1.jpg',             // 完整Banner图片路径（必需）
    alt: trans.banners.slide5.imageAlt,                 // 从翻译读取
    overlay: true,                                       // 是否显示遮罩层（可选，默认 false）
    overlayColor: 'bg-black/40',                        // 遮罩层颜色（可选，默认 'bg-black/40'）
  },
  
  // 文字内容（可选，如果不提供则不显示文字）
  overlayContent: {
    position: 'center',                                  // 文字位置：'center' | 'top-left' | 'top-right' | 'bottom-left' | 'bottom-right' | 'bottom-center'
    siteName: trans.banners.slide5.siteName,            // 从翻译读取（可选）
    mainTitle: trans.banners.slide5.mainTitle,          // 从翻译读取（可选）
    description: trans.banners.slide5.description,       // 从翻译读取（可选）
    button: {
      text: trans.banners.slide5.buttonText,             // 从翻译读取（可选）
      link: '/scenarios/marketing',                      // 按钮链接（配置）
      icon: 'fas fa-arrow-right'                         // 按钮图标（配置）
    }
  }
}
```

---

## 📝 修改流程（重要！）

### 🎯 两步修改流程

**步骤1：修改翻译文字** (`src/i18n/translations.ts`)
- 修改官网名称、主标题、描述、按钮文字、图片alt
- 用户只需提供简体中文，AI 自动生成英文、日文、繁体中文

**步骤2：修改配置** (`src/pages/HomepageDB.tsx`)
- 修改背景色、图片路径、按钮链接、按钮图标

---

### 示例 1：添加新 Banner（左文右图模式）

**用户输入**（只需提供简体中文）：
```
添加Banner第5页（左文右图模式）：
- 布局模式：左文右图
- 背景色：from-orange-50 via-yellow-50 to-amber-50
- 网格图案：否
- 官网名称：ZENAVA Marketing
- 主标题：智能营销自动化平台
- 概述：实时识别客户意图，智能分配高潜线索，基于会话分析优化营销策略，提升转化率降低获客成本
- 按钮文字：了解更多营销方案
- 按钮链接：/scenarios/marketing
- 按钮图标：fas fa-chart-line
- 右侧：图片
- 图片路径：/assets/images/marketing-automation.png
- 图片描述：营销自动化系统界面展示
```

**AI 操作**：
1. 在 `translations.ts` 的 `zh/en/jp/hk` 四个语言的 `banners` 中添加 `slide5` 配置
2. 在 `HomepageDB.tsx` 的 `bannerSlides` 数组中添加第5个元素，设置 `layout: 'split'`

---

### 示例 1-2：添加新 Banner（全图模式）

**用户输入**（只需提供简体中文）：
```
添加Banner第6页（全图模式）：
- 布局模式：全图
- 图片路径：/assets/images/banner-full-marketing.jpg
- 图片描述：营销活动Banner图
- 遮罩层：是（bg-black/40）
- 文字位置：居中
- 官网名称：ZENAVA Marketing
- 主标题：智能营销自动化平台
- 概述：实时识别客户意图，智能分配高潜线索，基于会话分析优化营销策略
- 按钮文字：了解更多营销方案
- 按钮链接：/scenarios/marketing
- 按钮图标：fas fa-chart-line
```

**AI 操作**：
1. 在 `translations.ts` 的 `zh/en/jp/hk` 四个语言的 `banners` 中添加 `slide6` 配置
2. 在 `HomepageDB.tsx` 的 `bannerSlides` 数组中添加第6个元素，设置 `layout: 'full-image'`

---

### 示例 2：修改现有 Banner 文字

**用户输入**：
```
修改Banner第1页的文字：
- 主标题：企业级AI对话解决方案（改进版）
- 概述：通过智能AI助手转变您的客户服务，提升客户体验，降低运营成本，实现业务增长
- 按钮文字：立即体验
```

**AI 操作**：
只需修改 `translations.ts` 中 `banners.slide1` 的对应字段（四种语言同步更新）

---

### 示例 3：修改现有 Banner 配置

**用户输入**：
```
修改Banner第2页的配置：
- 背景色：from-green-100 via-blue-100 to-purple-100
- 按钮链接：/new-link
- 按钮图标：fas fa-star
```

**AI 操作**：
只需修改 `HomepageDB.tsx` 中 `bannerSlides[1]` 的配置

---

### 示例 4：修改 Banner 为全图模式

**用户输入**：
```
将Banner第3页改为全图模式：
- 布局模式：全图
- 图片路径：/assets/images/new-banner.jpg
- 图片描述：新产品发布Banner
- 遮罩层：是
- 文字位置：底部居中
- 保留现有文字内容
```

**AI 操作**：
1. 修改 `HomepageDB.tsx` 中 `bannerSlides[2]`，将 `layout` 改为 `'full-image'`
2. 添加 `fullImage` 配置
3. 将 `leftContent` 改为 `overlayContent`，并设置 `position: 'bottom-center'`

---

## 💡 官网名称配置说明

### 🎯 重要提醒
- **官网名称（siteName）完全可自定义**，不一定是"ZENAVA"
- 可以根据不同Banner的内容设置不同的名称
- 支持纯中文、纯英文、或中英文混合

### 常见使用场景

| 场景 | 官网名称示例 | 说明 |
|-----|------------|-----|
| 品牌主页 | `ZENAVA` | 使用主品牌名 |
| 产品线页 | `ZENAVA Pro`、`ZENAVA Service` | 品牌名 + 产品线 |
| 中文品牌 | `智能AI平台`、`智能云服务` | 纯中文品牌名 |
| 混合品牌 | `ZENAVA 智能平台` | 中英文混合 |
| 子品牌 | `AI Assistant`、`Smart CRM` | 独立产品品牌 |

---

## 🎨 Banner组成部分详解

### 1. 背景配置 (background)

**常用渐变色组合**：

| 风格 | 渐变类名 | 效果 |
|-----|---------|-----|
| 科技紫 | `from-white via-[#f0ebff] to-[#5E3AFC]` | 科技感、专业 |
| 梦幻粉紫 | `from-blue-50 via-purple-50 to-pink-50` | 柔和、梦幻 |
| 清新蓝绿 | `from-green-50 via-teal-50 to-blue-50` | 清新、自然 |
| 温暖橙黄 | `from-orange-50 via-yellow-50 to-amber-50` | 温暖、活力 |
| 商务灰蓝 | `from-gray-50 via-blue-50 to-indigo-50` | 商务、稳重 |
| 深色科技 | `from-gray-900 via-purple-900 to-blue-900` | 高端、科技 |

### 2. 常用按钮文案

| 简体中文 | 英文 | 日文 | 繁体中文 | 用途 |
|---------|-----|------|---------|-----|
| 预约咨询 | Schedule Consultation | 相談を予約 | 預約諮詢 | 联系咨询 |
| 了解更多 | Learn More | 詳細を見る | 了解更多 | 查看详情 |
| 免费试用 | Start Free Trial | 無料トライアル | 免費試用 | 试用产品 |
| 立即体验 | Try Now | 今すぐ体験 | 立即體驗 | 快速体验 |
| 查看演示 | View Demo | デモを見る | 查看演示 | 演示功能 |

### 3. 常用图标

| 图标类名 | 图标 | 用途 |
|---------|-----|-----|
| `fas fa-phone` | 📞 | 联系、咨询 |
| `fas fa-arrow-right` | → | 了解更多、查看 |
| `fas fa-rocket` | 🚀 | 启动、试用 |
| `fas fa-play` | ▶️ | 播放、演示 |
| `fas fa-chart-line` | 📈 | 数据、营销 |
| `fas fa-headset` | 🎧 | 客服、支持 |

---

## ✅ 修改注意事项

### 必须遵守：
1. **四语同步**: 所有文字内容必须包含 zh、en、jp、hk 四个语言版本
2. **自动翻译**: 用户只提供简体中文，AI 自动生成其他三种语言
3. **ID唯一性**: 每个Banner的 `id` 必须唯一（如 slide-1, slide-2, slide-3）
4. **布局模式**: 必须明确指定 `layout: 'split'` 或 `layout: 'full-image'`
5. **官网名称**: `siteName` 完全可自定义，支持品牌名、产品名或任何标识，不一定是"ZENAVA"
6. **响应式**: 确保文字长度适中，避免移动端溢出
7. **图片优化**: 
   - 左文右图模式：图片文件大小 < 500KB
   - 全图模式：图片文件大小 < 1MB（建议 1920x1080 或更高分辨率）
8. **链接有效**: 确保所有链接可用，内部链接使用相对路径
9. **全图模式文字可读性**: 如果图片较亮，建议添加遮罩层（`overlay: true`）提高文字可读性

### ❌ 禁止操作：
1. ❌ 不要修改JSX结构和HTML标签
2. ❌ 不要修改CSS类名
3. ❌ 不要删除或修改轮播逻辑的JavaScript代码
4. ❌ 不要修改翻页指示器的结构
5. ❌ 不要使用过长的文字（主标题 > 20字，描述 > 100字）
6. ❌ 不要使用超过8个Banner（系统会自动截取最新8个）

### 📏 内容长度建议：

| 内容 | 简体中文 | 英文 | 说明 |
|-----|---------|-----|-----|
| 官网名称 | 2-10个汉字 | 1-5个单词 | 简短品牌名 |
| 主标题 | 8-15个汉字 | 3-8个单词 | 核心价值主张 |
| 概述 | 50-80个汉字 | 30-60个单词 | 详细描述 |
| 按钮文字 | 2-6个汉字 | 1-4个单词 | 简短行动号召 |
| 图片描述 | 5-15个汉字 | 3-10个单词 | 简要说明 |

---

## 🚀 测试验证步骤

修改后必须验证：

### 1. 多语言测试
- ✅ 在浏览器中检查**四种语言版本**
- ✅ 验证每种语言的文字长度不会破坏布局
- ✅ 检查按钮文字在不同语言下不溢出

### 2. 响应式测试（必须！）
- ✅ 切换到移动端视图（DevTools: Cmd/Ctrl+Shift+M）
- ✅ 测试 iPhone SE (375px)
- ✅ 测试 iPhone 12/13/14 (390px)
- ✅ 测试 iPad (768px)
- ✅ 测试桌面端 (1024px+)

### 3. 轮播功能测试
- ✅ 等待10秒，验证自动翻页工作
- ✅ 点击翻页指示器，验证手动跳转工作
- ✅ 验证点击后重新开始10秒计时
- ✅ 验证翻页动画流畅（渐变过渡）

---

## 🎓 特殊场景处理

### 场景1：只想显示1个Banner（不自动翻页）
**操作**：
- 删除其他Banner，只保留1个
- 系统会自动检测，不显示翻页指示器
- 自动翻页功能自动停用

### 场景2：修改自动翻页时间（默认10秒）
**操作**：
1. 找到代码行（约第 455 行）
2. 修改 `}, 10000);` 中的 `10000`
3. 单位：毫秒（5000 = 5秒，15000 = 15秒）

### 场景3：禁用自动翻页（保留手动翻页）
**操作**：
找到代码行（约第 405 行），改为：
```typescript
const autoPlayEnabled = false;
```

---

## 📞 技术支持

如有问题，请联系：
- 邮箱：marketing@zenava.ai
- 文档：`.cursor/rules/5-homepage-banner.mdc`
