---
alwaysApply: true
---
# Zenava 项目 - 头部导航栏修改规则

## 导航栏定位
- **文件路径**: `src/components/UnifiedNavigation.tsx`
- **配置文件**: `src/utils/navigation-helper.ts`
- **组件位置**: 所有页面顶部
- **组件功能**: 展示Logo、主导航菜单、语言切换器、CTA按钮
- **支持语言**: 简体中文(zh)、英文(en)、日文(jp)、繁体中文(hk)
- **主要输入语言**: 简体中文 (zh) - 其他语言自动生成

## 导航栏结构说明

### 🎨 整体布局
- **左侧**: Logo（可点击跳转首页）
- **中间**: 主导航菜单（支持链接和下拉菜单）
- **右侧**: 语言切换器 + CTA按钮（可选）
- **移动端**: 汉堡菜单 + 语言切换器

---

## 📍 导航栏组成部分

### 1. Logo配置

```typescript
{
  logo: {
    url: 'src/assets/zenava-logo-base64.ts',
    alt: { zh: 'ZENAVA Logo', en: 'ZENAVA Logo', jp: 'ZENAVA ロゴ', hk: 'ZENAVA Logo' },
    height: '2.5rem',           // Logo高度（默认 2.5rem = 40px）
    maxWidth: '200px',          // Logo最大宽度
    link: '/'                   // Logo点击跳转链接
  }
}
```

---

### 2. 主导航菜单项

**主菜单类型**:
- **链接类型**（`type: 'link'`）：直接跳转的菜单项
- **下拉类型**（`type: 'dropdown'`）：带二级菜单的下拉菜单

#### 链接类型菜单项

```typescript
{
  id: 'menu-1',
  type: 'link',
  label: {
    zh: '【菜单名称-简体中文】',
    en: '【AI自动生成】',
    jp: '【AI自动生成】',
    hk: '【AI自动生成】'
  },
  url: '/path/to/page',
  icon: 'fas fa-home',  // 可选，Font Awesome 图标
  order: 1,             // 显示顺序
  visible: true,        // 是否显示
  target: '_self'       // '_self' 或 '_blank'
}
```

#### 下拉类型菜单项

```typescript
{
  id: 'menu-2',
  type: 'dropdown',
  label: {
    zh: '【下拉菜单名称-简体中文】',
    en: '【AI自动生成】',
    jp: '【AI自动生成】',
    hk: '【AI自动生成】'
  },
  icon: 'fas fa-th',  // 可选
  order: 2,
  visible: true,
  children: [
    // 二级菜单项（见下方）
  ]
}
```

---

### 3. 二级导航菜单（下拉菜单子项）

```typescript
{
  children: [
    {
      id: 'submenu-1',
      label: {
        zh: '【子菜单标题-简体中文】',
        en: '【AI自动生成】',
        jp: '【AI自动生成】',
        hk: '【AI自动生成】'
      },
      url: '/path/to/subpage',
      icon: 'fas fa-rocket',  // 必需，Font Awesome 图标
      order: 1,
      visible: true,
      target: '_self'
    }
  ]
}
```

**重要规范**:

1. **内容块组成**: 
   - ✅ 每个子菜单项必须包含**图标**和**名称**
   - ✅ 图标使用 Font Awesome 图标类（如 `fas fa-robot`）
   - ✅ 名称支持四种语言版本（zh、en、jp、hk）
   - ⚠️ **不包含描述文字**：子菜单只显示图标+名称，不显示描述

2. **名称长度限制**:
   - ✅ 最大宽度：**150px**
   - ✅ 超出限制：使用 CSS `text-overflow: ellipsis` 截断，不显示超出文本
   - ✅ 建议长度：
     - 简体中文：7-10个汉字
     - 英文：10-15个字符
     - 日文：7-10个字符
     - 繁体中文：7-10个字符

3. **分列展示规则**:
   - ✅ **每列最多5个子项**
   - ✅ 超过5个子项时，自动分成多列展示
   - ✅ 示例：
     - 1-5个子项：单列显示（384px宽）
     - 6-10个子项：分成2列（600px宽）
     - 11-15个子项：分成3列（900px宽）
   - ✅ 布局：使用 CSS Grid 实现多列布局

4. **链接跳转行为**:
   - ✅ 默认行为：**在原窗口打开**（`target="_self"`）
   - ✅ 外部链接：如需新窗口打开，显式设置 `target="_blank"`
   - ✅ 安全性：外部链接必须添加 `rel="noopener noreferrer"`

---

## 📝 用户输入格式（简化版）

### 示例 1：添加新菜单项（链接类型）

```
添加导航菜单：
- 菜单名称：关于我们
- 菜单类型：链接
- 链接地址：/about
- 图标：fas fa-building
- 显示顺序：3
```

### 示例 2：添加下拉菜单

```
添加下拉菜单：
- 菜单名称：产品
- 菜单类型：下拉
- 图标：fas fa-th
- 显示顺序：1
- 子菜单：
  1. AI Agents → /products/ai-agents（图标：fas fa-robot）
  2. 传讯与即时对话 → /products/messaging（图标：fas fa-comments）
  3. Voice → /products/voice（图标：fas fa-microphone）
```

---

## ✅ 修改注意事项

### 必须遵守：
1. **四语同步**: 所有文字内容必须包含 zh、en、jp、hk 四个语言版本
2. **自动翻译**: 用户只提供简体中文，AI 自动生成其他三种语言
3. **ID唯一性**: 每个菜单项的 `id` 必须唯一
4. **显示顺序**: 使用 `order` 字段控制菜单项显示顺序（数字越小越靠前）
5. **可见性**: 使用 `visible` 字段控制菜单项是否显示
6. **品牌名称**: "ZENAVA" 不翻译，保持统一
7. **链接有效**: 确保所有链接可用，内部链接使用相对路径
8. **响应式**: 确保移动端正常显示
9. **图标必需**: 每个子菜单项必须包含图标（Font Awesome）和名称
10. **名称宽度**: 子菜单名称最大宽度150px，超出部分使用 `text-overflow: ellipsis` 截断
11. **分列展示**: 每列最多5个子项，超过5个自动分成多列展示
12. **原窗口打开**: 默认使用 `target="_self"` 在原窗口打开链接
13. **无描述文字**: 子菜单只显示图标+名称，不显示描述文字

### ❌ 禁止操作：
1. ❌ 不要修改组件的TypeScript类型定义
2. ❌ 不要删除响应式类名（md:、lg: 等）
3. ❌ 不要修改下拉菜单的动画逻辑
4. ❌ 不要破坏移动端汉堡菜单功能
5. ❌ 不要删除语言切换器（除非明确要求）

### 📏 内容长度建议：
- **菜单名称**: 2-6个汉字（英文 1-4 个单词）
- **子菜单名称**: 4-10个汉字（英文 2-6 个单词）
  - ⚠️ **最大宽度限制**: 150px（中英日韩统一标准）
  - ⚠️ **超出处理**: 自动截断，显示省略号（`text-overflow: ellipsis`）
  - ✅ **推荐长度**: 简体中文 7-10字，英文 10-15字符
  - ⚠️ **不包含描述**：子菜单只显示名称，不显示描述文字
- **CTA按钮文字**: 2-6个汉字（英文 1-4 个单词）

---

## 🎨 常用图标（Font Awesome）

```typescript
// 产品类
'fas fa-robot'          // AI、机器人
'fas fa-comments'       // 消息、对话
'fas fa-phone'          // 电话、语音
'fas fa-th'             // 产品、应用

// 场景类
'fas fa-bullhorn'       // 营销
'fas fa-chart-line'     // 销售
'fas fa-headset'        // 客服
'fas fa-users-cog'      // 管理

// 行业类
'fas fa-shopping-cart'  // 零售
'fas fa-car'            // 汽车
'fas fa-laptop-code'    // 软件
'fas fa-hotel'          // 饭店旅游

// 资源类
'fas fa-book'           // 博客、文档
'fas fa-video'          // 视频
'fas fa-file-alt'       // 文章
'fas fa-chart-bar'      // 报告
```

---

## 🚀 测试验证步骤

### 1. 多语言测试
- ✅ 切换到**四种语言**验证菜单文字
- ✅ 验证每种语言的文字长度不会破坏布局

### 2. 响应式测试（必须！）
- ✅ 切换到移动端视图（DevTools: Cmd/Ctrl+Shift+M）
- ✅ 测试 iPhone SE (375px)
- ✅ 测试 iPhone 12/13/14 (390px)
- ✅ 测试 iPad (768px)
- ✅ 测试桌面端 (1024px+)

### 3. 菜单功能测试
- ✅ 点击Logo跳转首页
- ✅ 主菜单链接正确跳转
- ✅ 下拉菜单悬停正常显示
- ✅ 子菜单项可点击
- ✅ 每列最多5个子项，超过5个自动分成多列
- ✅ 子菜单项图标和名称正常显示，无描述文字
- ✅ 子菜单名称最大宽度150px，超出部分自动截断

---

## 📞 技术支持

如有问题，请联系：
- 邮箱：marketing@zenava.ai
- 文档：`.cursor/rules/7-navigation.mdc`
