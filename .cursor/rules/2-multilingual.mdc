---
alwaysApply: false
---
# Zenava 项目 - 多语言处理规则

## 多语言处理
- 所有用户可见文本必须通过 `src/i18n/translations.ts` 管理
- 支持的语言代码：`zh` (简体中文)、`en` (英文)、`hk` (繁体中文)、`jp` (日文)
- **主要输入语言**: 中文简体 (zh)
- **自动翻译机制**: 输入简体中文后，自动生成其他三种语言
- 新增文本必须同时包含四种语言的翻译
- 使用 `getTranslations(lang)` 工具函数获取翻译

## 🤖 自动翻译工作流程

**当用户提供中文简体文案时：**
1. ✅ 接收用户输入的简体中文内容
2. 🤖 自动生成英文 (en) 翻译
3. 🤖 自动生成日文 (jp) 翻译
4. 🤖 自动生成繁体中文 (hk) 翻译
5. 📝 同时更新所有四种语言版本
6. ✅ 保持专业术语和品牌名称的一致性

**翻译质量标准：**
- 英文：符合国际企业级标准，参考 Salesforce、HubSpot、Zendesk 等
- 日文：使用敬语和商务用语，符合日本企业习惯
- 繁体中文：使用香港/台湾商务用语，避免大陆用语
- 保持技术术语的专业性（AI、KPI、Dashboard 等）

## AI辅助编码 - 添加翻译

### 当我说"添加翻译"时
1. 用户只需提供**简体中文**文案
2. AI 自动生成 `en`、`jp`、`hk` 三个语言版本
3. 编辑 `src/i18n/translations.ts`
4. 在对应的嵌套对象中添加 `zh`、`en`、`jp`、`hk` 四个语言版本
5. 保持翻译结构一致
6. 使用专业术语（参考 Avaya、Genesys 等行业标准）

**示例：**
```typescript
// 用户输入："添加翻译：实时数据分析"
// AI 自动生成并添加：
scenarios: {
  newFeature: {
    zh: '实时数据分析',
    en: 'Real-time Data Analytics',
    jp: 'リアルタイムデータ分析',
    hk: '實時數據分析'
  }
}
```

## 文案修改模板

```typescript
// ✅ 正确的文案修改（包含四种语言）
{language === 'zh' && '新的简体中文文案'}
{language === 'en' && '新的英文文案'}
{language === 'jp' && '新的日文文案'}
{language === 'hk' && '新的繁体中文文案'}

// 🤖 用户只需提供简体中文，AI 自动生成其他语言
用户输入: "修改标题为：智能商业分析平台"
AI 自动生成:
{language === 'zh' && '智能商业分析平台'}
{language === 'en' && 'Intelligent Business Analytics Platform'}
{language === 'jp' && 'インテリジェントビジネス分析プラットフォーム'}
{language === 'hk' && '智能商業分析平台'}

// ❌ 错误的修改（破坏了结构）
<h1>新的标题</h1>  // 这会破坏多语言逻辑
```

## 🏗️ 翻译数据架构原则

### ⚠️ 关键原则：翻译统一管理
- **所有翻译数据必须集中在 `src/i18n/translations.ts` 中管理**
- **禁止在组件中硬编码多语言嵌套对象**
- **翻译数据与配置数据必须分离**

### 📦 数据分离原则

#### ✅ 正确的架构
```typescript
// 1️⃣ 翻译文件 (src/i18n/translations.ts)
export const siteTranslations = {
  zh: {
    banners: {
      slide1: {
        siteName: 'ZENAVA',
        mainTitle: '企业级AI对话解决方案',
        description: '通过智能AI助手转变...',
        buttonText: '预约咨询'
      }
    }
  },
  en: { banners: { slide1: { ... } } },
  jp: { banners: { slide1: { ... } } },
  hk: { banners: { slide1: { ... } } }
}

// 2️⃣ 组件文件 (使用翻译引用)
const bannerConfig = {
  id: 'slide-1',
  background: { gradient: '...', pattern: true },  // 配置数据
  content: {
    siteName: trans.banners.slide1.siteName,       // 翻译引用
    mainTitle: trans.banners.slide1.mainTitle,     // 翻译引用
    description: trans.banners.slide1.description, // 翻译引用
    button: {
      text: trans.banners.slide1.buttonText,       // 翻译引用
      link: '#contact',                            // 配置数据
      icon: 'fas fa-phone'                         // 配置数据
    }
  }
}

// 3️⃣ 显示时直接使用
<h1>{bannerConfig.content.mainTitle}</h1>  // 自动显示对应语言
```

#### ❌ 错误的架构（禁止）
```typescript
// ❌ 在组件中硬编码多语言嵌套对象
const bannerConfig = {
  content: {
    mainTitle: {
      zh: '企业级AI对话解决方案',
      en: 'Enterprise AI Solutions',
      jp: '企業向けAIソリューション',
      hk: '企業級AI對話解決方案'
    }
  }
}

// ❌ 显示时需要多次判断
{language === 'zh' && bannerConfig.content.mainTitle.zh}
{language === 'en' && bannerConfig.content.mainTitle.en}
{language === 'jp' && bannerConfig.content.mainTitle.jp}
{language === 'hk' && bannerConfig.content.mainTitle.hk}
```

### 🎯 实例：Banner 翻译架构

**迁移前的问题**：
- ❌ 翻译数据分散在 `HomepageDB.tsx` 组件中（约200行）
- ❌ 每个字段都包含 `{zh: '...', en: '...', jp: '...', hk: '...'}` 嵌套结构
- ❌ 修改翻译需要在组件内部找到对应位置
- ❌ 翻译一致性难以保证

**迁移后的优势**：
- ✅ 所有翻译统一在 `translations.ts` 中管理
- ✅ 组件代码减少 49%（187行 → 95行）
- ✅ 翻译和配置清晰分离
- ✅ 修改翻译只需在一个地方操作
- ✅ 易于对比和统一术语

### 📋 翻译数据 vs 配置数据

| 数据类型 | 存放位置 | 示例 | 说明 |
|---------|---------|------|------|
| **翻译数据** | `translations.ts` | 标题、描述、按钮文字、图片alt | 随语言变化的文本 |
| **配置数据** | 组件文件 | 背景色、图片路径、链接、图标 | 不随语言变化的配置 |

### ✅ 如何判断是翻译还是配置

**翻译数据**（需要多语言）：
- ✅ 用户可见的文字内容
- ✅ 图片/视频的 alt 描述
- ✅ 按钮/链接的文字
- ✅ 提示信息、错误信息

**配置数据**（单一值）：
- ✅ CSS 类名、颜色、尺寸
- ✅ 图片/视频的路径
- ✅ 链接的 URL
- ✅ Font Awesome 图标类名
- ✅ 布尔值配置（true/false）

## 测试验证

**语言切换测试路径：**
- 简体中文：`http://localhost:5173` 或 `/zh/...`
- 英文：`http://localhost:5173/en` 或 `/en/...`
- 日文：`http://localhost:5173/jp` 或 `/jp/...`
- 繁体中文：`http://localhost:5173/hk` 或 `/hk/...`

**测试清单**：
1. ✅ 在浏览器中检查**四种语言版本**
2. ✅ 验证每种语言的文字长度不会破坏布局
3. ✅ 检查专业术语的一致性
4. ✅ 验证自动翻译的质量和准确性
5. ✅ 确保品牌名称 "Zenava" 不被翻译
6. ✅ 确认所有翻译数据已迁移到 `translations.ts`
7. ✅ 确认组件中无硬编码的多语言嵌套对象
